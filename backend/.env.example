# Copy to backend/.env (git-ignored) and fill in. Never commit the real file.
FRI_CAMERA_HOST=192.168.7.2
# RTSP stream account: web UI > Settings > Video settings > Authentication (user is 'rtsp';
# set its password there). This is NOT the admin web login.
FRI_RTSP_USER=rtsp
FRI_RTSP_PASSWORD=
