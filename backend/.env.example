# Copy to backend/.env (git-ignored) and fill in. Never commit the real file.
FRI_CAMERA_HOST=192.168.7.2
# Camera web-UI login from the calibration certificate (also used by RTSP)
FRI_RTSP_USER=admin
FRI_RTSP_PASSWORD=
